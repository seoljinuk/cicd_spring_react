**********************************************************************************
# 스프링 기본기 익히기
# src/main/java 폴더
**********************************************************************************
다음 패키지들을 생성하도록 합니다.
common
config
constant
controller
dto
entity
handler
repository
service
test
**********************************************************************************
# src/main/resources 폴더
**********************************************************************************
application.properties	환경 설정(포트번호, DB 연결 정보 등)
static	CSS, JS, 이미지 같은 정적 파일
templates	Thymeleaf 같은 템플릿 파일(HTML)
**********************************************************************************
# pom.xml
**********************************************************************************
<dependency>
   <groupId>org.springframework.boot</groupId>
   <artifactId>spring-boot-starter-data-jpa</artifactId>
</dependency>

<dependency>
   <groupId>org.springframework.boot</groupId>
   <artifactId>spring-boot-starter-web</artifactId>
</dependency>

<dependency>
   <groupId>org.springframework.boot</groupId>
   <artifactId>spring-boot-starter-web-services</artifactId>
</dependency>

<dependency>
   <groupId>org.springframework.boot</groupId>
   <artifactId>spring-boot-devtools</artifactId>
   <scope>runtime</scope>
   <optional>true</optional>
</dependency>

<dependency>
   <groupId>com.h2database</groupId>
   <artifactId>h2</artifactId>
   <scope>runtime</scope>
</dependency>

<dependency>
   <groupId>com.mysql</groupId>
   <artifactId>mysql-connector-j</artifactId>
   <scope>runtime</scope>
</dependency>

<dependency>
   <groupId>org.projectlombok</groupId>
   <artifactId>lombok</artifactId>
   <optional>true</optional>
</dependency>

<dependency>
   <groupId>org.springframework.boot</groupId>
   <artifactId>spring-boot-starter-test</artifactId>
   <scope>test</scope>
</dependency>
**********************************************************************************
# application.properties
**********************************************************************************
spring.devtools.restart.enabled=true
spring.devtools.livereload.enabled=true
productImageLocation=C:\\shop\\images
uploadPath=file:///C:/shop/images/
spring.datasource.url=jdbc:mysql://localhost:3306/coffee?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=mysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.properties.hibernate.show_sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.type.descriptor.sql=trace
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
server.servlet.session.timeout=120m
**********************************************************************************
# Automatic Restart 기능 활성화
Automatic Restart 구현 절차
1.	pom.xml(spring-boot-devtools 항목이 이미 설정되어 있으므로 생략)
2.	application.properties 파일(이미 설정되어 있으므로 생략)
3.	IntelliJ 설정
4.	브라우저 확인

3.	IntelliJ 설정
IntelliJ 설정 창에서 Advanced Settings 메뉴로 이동한 뒤, Compiler 항목을 선택합니다.
그곳에서 Allow auto-make to start ... 옵션을 활성화(체크)하도록 합니다.

설정 항목에서 Build, Execution, Deployment 메뉴로 이동한 후, Compiler 항목을 선택합니다.
그 안에서 Build project automatically 옵션을 찾아 체크하도록 합니다.

테스트 하기
CoffeeApplication.java 파일 실행 후 index.html 파일을 다음과 같이 수정해 봅니다.
**********************************************************************************
# 롬복(Lombok) 사용
롬복 구현 절차
1.	pom.xml(의존성 설정이 이미 되어 있으므로 생략)
2.	인텔리제이 설정

step01. Lombok 플러그인 설치
Lombok 플러그인은 IDE(인텔리 제이)가 Lombok이 생성한 메소드/생성자를 인식할 수 있게 해주는 역할을 합니다.
설정 메뉴에서 Plugins 영역의 Marketplace에서 'lombok'로 검색을 하고, Install 버튼을 클릭합니다.

step02. annotation processing 설정
자바에서는 @Override, @Deprecated 같은 기본 어노테이션뿐 아니라, 개발자가 정의한 어노테이션을 사용할 수 있습니다. 그런데 어떤 어노테이션은 단순히 표시 역할만 하는 게 아니라, 컴파일 시점에 코드를 새로 생성하거나 수정하는 기능을 합니다. 이를 담당하는 것이 `어노테이션 프로세서(annotation processor)`입니다.

설정 메뉴의 Build, Execution, Deployment → Compiler → Annotation Processors → Enable annotation processing
을 켜 주면, IDE가 컴파일 시 Lombok 같은 어노테이션 프로세서를 실행하도록 허용하는 겁니다.
**********************************************************************************
# 클래스(엔터티) 작성
Fruit.java	과일 1개를 표현하는 자바 클래스
FruitController.java	과일과 관련된 웹 요청에 대하여 데이터를 표현해 주는 컨트롤러 클래스

# Fruit.java
package com.coffee.entity;

import lombok.Getter;
import lombok.Setter;
import lombok.ToString;

@Getter @Setter @ToString
public class Fruit {
    private String id;
    private String name;
    private int price;

    public Fruit(){}

    public Fruit(String id, String name, int price) {
        this.id = id;
        this.name = name;
        this.price = price;
    }
}

# FruitController.java
package com.coffee.controller;

import com.coffee.entity.Fruit;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.ArrayList;
import java.util.List;

@RestController // 컨트롤러는 특정 요청에 대한 처리를 수행해 줍니다.
public class FruitController {
    @GetMapping("/fruit")
    public Fruit test(){
        Fruit bean = new Fruit();
        bean.setId("banana");
        bean.setName("바나나");
        bean.setPrice(1000);
        return bean ;
    }

    @GetMapping("/fruit/list")
    public List<Fruit> test02(){
        List<Fruit> fruitList = new ArrayList<>();
        fruitList.add(new Fruit("apple", "사과", 1000));
        fruitList.add(new Fruit("pear", "나주배", 2000));
        fruitList.add(new Fruit("grape", "포도", 3000));
        return fruitList ;
    }
}
**********************************************************************************
# JSON Viewer
검색 키워드 : json viewer chorem
JSON Viewer란 크롬(Chrome) 브라우저에서 JSON 데이터를 보기 좋게 표시해 주는 확장 프로그램(Extension) 입니다.
**********************************************************************************
브라우저에서 확인
모든 파일에 대한 코드 작성이 완료되었으므로, 브라우저에서 다음과 같이 테스트를 진행해 봅니다.
Fruit.java
http://localhost:9000/fruit
다음은 과일 1개에 대한 정보를 요청하는 화면입니다.
Controller 클래스의 test() 메소드에 해당합니다.

FruitController.java
http://localhost:9000/fruit/list
다음은 여러 개의 과일 정보를 요청하는 화면입니다.
Controller 클래스의 test02() 메소드가 여기에 해당합니다.
**********************************************************************************
# 리액트 기본기 익히기
**********************************************************************************
항목	설명
스프링에서 이미 구현했던 기능 목록
/fruit	과일 하나(Fruit)의 정보 반환 (JSON Object)
/fruit/list	과일 여러 개(List<Fruit>)의 정보 반환 (JSON Array)
**********************************************************************************
구현할 파일 목록
폴더	파일	설명
src/config/config.tsx	
호스트 컴퓨터의 이름(127.0.0.1)과 스프링 부트의 포트 번호를 변수 형태로 정의 해 놓은 파일입니다.
여러 개의 파일에서 공용 형태로 사용이 됩니다.

# 코드 작성
// 이 파일은 설정용 파일입니다.
const API_HOST = "localhost";  // 호스트 컴퓨터 이름(127.0.0.1)

const API_PORT = "9000"; // 스프링 부트 포트 번호

// export 키워드를 적어 주어야 외부에서 접근 가능합니다.
export const API_BASE_URL = `http://${API_HOST}:${API_PORT}`;
**********************************************************************************
src/main.tsx
React 프로젝트의 시작점(entry point) 파일입니다.
이전 버전에서는 'index.tsx'라는 이름으로 사용이 되었습니다.

# 코드 작성
import ReactDOM from 'react-dom/client'
import { BrowserRouter } from 'react-router-dom'
import App from './App';

import 'bootstrap/dist/css/bootstrap.min.css'; // (step01) for 부트 스트랩

ReactDOM.createRoot(document.getElementById('root')!).render(
  <BrowserRouter>
    <App />
  </BrowserRouter>
)
**********************************************************************************
src/ui/MenuItems.tsx	리액트에서 사용하는 상단 영역의 메뉴 정보를 저장해 놓은 파일입니다.

# 코드 작성
import { NavDropdown, Navbar, Container, Nav } from "react-bootstrap";

import { useNavigate } from "react-router-dom";

function App() {
   const navigate = useNavigate();

   return (
      <Navbar bg="dark" variant="dark" expand="lg">
         <Container>
            <Nav className="me-auto">
               <NavDropdown title={`기본 연습`}>
                  <NavDropdown.Item onClick={() => navigate(`/fruit`)}>과일 1개</NavDropdown.Item>
                  <NavDropdown.Item onClick={() => navigate(`/fruit/list`)}>과일 목록</NavDropdown.Item>
               </NavDropdown>
            </Nav>
         </Container>
      </Navbar >
   );
}

export default App;
**********************************************************************************
Fruit.ts    타입 스크립트 인터페이스 작성

# 코드 작성
export interface Fruit {
    id: string
    name: string
    price: number
}
**********************************************************************************
FruitOne.tsx
해당 컴포넌트는 Spring Boot API(/fruit)에서 과일 데이터를 가져와 React 상태(state)에 저장한 뒤
Bootstrap 테이블을 사용해 화면에 예쁘게 출력하는 기능을 합니다.

# 코드 작성
import axios from "axios"

import { useEffect, useState } from "react";
import { API_BASE_URL } from "../config/config";
import { Table } from "react-bootstrap";

import type { Fruit } from "../types/Fruit"

// axios 라이브러리를 이용하여 리액트에서 스프링으로 데이터를 요청해야 합니다.
function App() {
    // Fruit 타입으로 상태를 지정하세요.
    // 처음에는 값이 없으므로 null, 데이터가 들어 오면 Fruit 타입
    // const [state변수, state변경함수] = useState<타입>(초기값);
    const [fruit, setFruit] = useState<Fruit | null>(null); // 넘겨 받은 과일 1개

    useEffect(() => { // BackEnd 서버에서 데이터 읽어 오기
        const fetchFruit = async () => {
            try {
                const url = `${API_BASE_URL}/fruit`; // 요청할 url 주소
                const response = await axios.get<Fruit>(url, {
                    withCredentials: true,
                });
                setFruit(response.data);
            } catch (error) {
                console.error(error);
            }
        };

        fetchFruit();
    }, []);

    return (
        <>
            <Table hover style={{ margin: '20px' }}>
                <tbody>
                    <tr>
                        <td>아이디</td>

                        {/* optional chaining은 객체가 null 또는 undefined일 때 오류 없이 접근하도록 하는 자바 스크립트 문법입니다.*/}
                        {/* optional chaining : fruit가 null → 아무것도 안 나옴(undefined 반환), fruit가 존재 → id 출력 */}
                        <td>{fruit?.id}</td>
                    </tr>
                    <tr>
                        <td>상품명</td>
                        <td>{fruit?.name}</td>
                    </tr>
                    <tr>
                        <td>단가</td>
                        <td>{fruit?.price.toLocaleString()}원</td>
                    </tr>
                </tbody>
            </Table >
        </>
    );
}

export default App;
**********************************************************************************
FruitList.tsx
해당 컴포넌트는 Spring Boot의 /fruit/list API에서 과일 목록을 불러와,
React-Bootstrap 테이블을 이용해 아이디, 상품명, 단가를 화면에 깔끔하게 보여줍니다.

# 코드 작성
import axios from "axios";

import { useEffect, useState } from "react";
import { Table } from "react-bootstrap";
import { API_BASE_URL } from "../config/config";
import type { Fruit } from "../types/Fruit"

function App() {
    // <Fruit[]>는 Fruit 객체들의 배열, ([])는 초기값이 빈 배열이라는 뜻입니다.
    const [fruitList, setFruitList] = useState<Fruit[]>([]); // 넘어온 과일 목록
    const url = `${API_BASE_URL}/fruit/list`;

    useEffect(() => {
        const fetchData = async () => {
            try { // axios에 제네릭 타입 추가
                const response = await axios.get<Fruit[]>(url);
                setFruitList(response.data);
            } catch (error) {
                console.error(error);
            }
        };

        fetchData();
    }, []);

    return (
        <>
            <Table hover style={{ margin: '20px' }}>
                <thead>
                    <tr>
                        <th>아이디</th>
                        <th>상품명</th>
                        <th>단가</th>
                    </tr>
                </thead>
                <tbody>
                    {fruitList.map((fruit) =>
                        <tr key={fruit.id}>
                            <td>{fruit.id}</td>
                            <td>{fruit.name}</td>
                            <td>{fruit.price.toLocaleString()} 원</td>
                        </tr>
                    )}
                </tbody>
            </Table >
        </>
    );
}

export default App;
**********************************************************************************
src/routes/AppRoutes.tsx	리액트에서 사용할 라우팅 정보를 담고 있는 파일입니다.

# 코드 작성
import { Route, Routes } from "react-router-dom";

import FruitOne from './../pages/FruitOne'; // 표현식01
import FruitList from './../pages/FruitList';

function App() {
    return (
        <Routes>
            <Route path='/fruit' element={<FruitOne />} />
            <Route path='/fruit/list' element={<FruitList />} />
        </Routes>
    );
}

export default App;
**********************************************************************************
src/App.tsx
React 프로젝트에서 핵심 컴포넌트를 정의하는 파일입니다.
상단 Navbar, 하단 Footer 등을 구현합니다.

# 코드 작성
import './App.css';

// 분리된 리액트 컴포넌트 import
import MenuItems from './ui/MenuItems';
import AppRouters from './routes/AppRoutes';

function App() {
  const appName = "IT Academy Coffee Shop";

  return (
    <>
      <MenuItems />

      {/* 분리된 라우터 정보 */}
      <AppRouters />

      <footer className="bg-dark text-light text-center py-3 mt-5">
        <p>&copy; 2025 {appName}. All rights reserved.</p>
      </footer>
    </>
  );
}

export default App;
**********************************************************************************
브라우저 확인
실행 결과를 보면 CORS 오류가 발생했음을 알려 주면서 데이터를 제대로 읽어 오지 못합니다.
"과일 목록"이라는 메뉴 역시 CORS 오류가 발생했음을 알려 주면서 데이터를 제대로 읽어 오지 못합니다.
이제 CORS가 무엇인지 좀 살펴 보고 해결해 보도록 하겠습니다.
**********************************************************************************
CORS
CORS(Cross-Origin Resource Sharing, 교차 출처 리소스 공유) 라고 부르며, 웹 브라우저에서 다른 도메인(출처)의 리소스(데이터, 이미지, API 등)에 접근할 수 있도록 허용/제어하는 보안 메커니즘입니다.
웹 브라우저는 보안상 기본적으로 동일 출처 정책(Same-Origin Policy, SOP)을 따릅니다.
즉 포트 번호가 동일하지 않으면 접속을 거부합니다.

사용 예시
	http://localhost:3000 → http://localhost:3000/api/data (같은 출처 → 허용)
	http://localhost:3000 → http://localhost:9000/api/data (포트 다름 → 차단)
	http://myapp.com → http://api.myapp.com (서브 도메인 달라서 차단)
**********************************************************************************
CORS 설정
WebConfig 클래스는 @Configuration 어노테이션에 의하여 Spring Boot의 설정 클래스로 등록되어 있고,
WebMvcConfigurer를 상속 받아 addCorsMappings를 오버라이딩하여 React(3000번 포트)에서 들어 오는
모든 요청을 허용하도록 세부 규칙을 정하고 있습니다.

# 코드 작성
package com.coffee.config;

import org.springframework.context.annotation.Configuration;
import org.springframework.web.servlet.config.annotation.CorsRegistry;
import org.springframework.web.servlet.config.annotation.WebMvcConfigurer;

@Configuration
public class WebConfig implements WebMvcConfigurer {
    @Override
    public void addCorsMappings(CorsRegistry registry) {
        // 3000번 포트에서 GET부터 PATCH까지의 열거한 요청들을 모두 수락하겠습니다.
        registry.addMapping("/**") /* 모든 경로 허용  */
                .allowedOrigins("http://localhost:5173") /* react 포트 */
                .allowedMethods("GET", "POST", "PUT", "DELETE", "OPTIONS", "PATCH")  /* 허용할 메소드 */
                .allowCredentials(true) ; // 쿠키 전송 허용
    }
}
**********************************************************************************
홈 페이지 구현 절차
이미지 파일 복사
압축 파일 "public_images_폴더이미지.zip" 파일을 압축해제하고, 해당 파일들을 C 드라이브에 shop\images 폴더에 복사합니다.
shop\images 폴더가 존재하지 않으면 직접 생성해 주어야 합니다.
**********************************************************************************
이미지 경로 설정
파일 : application.properties
key의 이름이 "uploadPath"인 항목이 파일 업로드 경로입니다.
다음과 같이 이미 구현 되어 있습니다.
uploadPath=file:///C:/shop/images/
**********************************************************************************
WebConfig 클래스
오버라이딩되어 있는 addResourceHandlers() 메소드는 외부에서 "/images/"으로 시작하는 요청이 들어 오면,
요청을 실제 서버의 로컬 경로와 연결시켜 주는 역할을 합니다.
예를 들어, 사용자가 http://localhost:9000/images/coffee1.jpg 와 같은 주소로 이미지를 요청하면,
Spring Boot는 이 요청을 실제 서버 내부의 폴더(예: C:/shop/images/coffee1.jpg) 에서 찾아 반환합니다.

# 코드 작성
// Value 클래스 : import org.springframework.beans.factory.annotation.Value;

@Value("${uploadPath}")
private String uploadPath ; // file:///C:/shop/images/

@Override
public void addResourceHandlers(ResourceHandlerRegistry registry) {
   registry
         .addResourceHandler("/images/**")
         .addResourceLocations(uploadPath);
}
**********************************************************************************
MenuItems.tsx
App.tsx 파일에서 넘겨 준 appName 프롭스를 함수의 매개 변수란에 작성합니다.
그리고, 네비게이션 바(Navbar) 컴포넌트 안에서 사이트의 로고나 제목 부분을 표시하기 위하여
<Navbar.Brand /> 코드를 추가합니다.
그리고, 홈 페이지로 이동하기 위하여 href='/'를 지정하도록 합니다.

# 코드 작성
// TypeScript : Props 타입 정의하기
type MenuItemsProps = {
    appName: string;
};

function App({ appName }: MenuItemsProps) {
    ...
            <Container>
                <Navbar.Brand href='/'>{appName}</Navbar.Brand>
                ...
            </Container>
    ...
}
**********************************************************************************
홈 페이지 작성
사용자가 웹 사이트에 처음 접속했을 때 볼 수 있는 대표 상품 소개용 메인 화면(Home Page)을
pages 폴더 아래에 `HomePage.tsx` 파일로 작성하도록 하겠습니다.
홈페이지에서 작성된 코드는 react-bootstrap 라이브러리를 이용해 이미지 슬라이드(Carousel)
형태의 메인 화면을 구현하였습니다. 서버에서 상품 정보를 불러와(axios 사용) 이미지와 간단한 설명을 화면에 표시하였습니다.

# 코드 작성 : `HomePage01.txt` 파일 참고
**********************************************************************************
AppRoutes.tsx

# 코드 작성
<Route path='/' element={<HomePage />} />
이외에 관련된 코드들을 작성하면 됩니다.
**********************************************************************************
브라우저 확인
모든 코드들을 모두 작성하고, 리액트를 구동시키면 브라우저에서 다음과 같은 화면을 확인할 수 있습니다.
브라우저에서 5개의 이미지가 Carousel 형태로 표시되며, 일정 시간마다 자동으로 슬라이드가 전환됩니다.
**********************************************************************************
