Name	Secret	비고
DOCKER_USERNAME	ugcadman	Docker Hub의 개인 계정
DOCKER_PASSWORD	dckr_pat_xxxxxxx	Docker Hub에서 발급 받은 Access Token
EC2_HOST	hello(차후 수정 예정)	Public IP 또는 Elastic IP
EC2_KEY	world(차후 수정 예정)	key-pair 파일(*.pem)의 내용 내용
DOCKER_SPRING_IMAGE	ugcadman/spring-img:latest	Docker Hub에 빌드할 이미지 이름
DOCKER_REACT_IMAGE	ugcadman/react-img:latest
DOCKER_SPRING_CONTAINER	spring-react-ctr	Image로 부터 생성되는 Container 이름


# EC2에서 할일
mkdir /home/ubuntu/spring_react

cd /home/ubuntu/spring_react

docker-compose.yml 파일 복사하기





git init 

git config --global core.autocrlf true

git config --global core.safecrlf false

git config user.name "cicdman"

git config user.email "cicdman@naver.com"

git remote add origin https://github.com/seoljinuk/cicd_spring_react.git

git add -A

git commit -m "ci/cd project"

git push -u origin master

수정한 다음 
git add -A

git commit -m "ci/cd project"

git push

을 수행합니다.

GitHub는 보안을 위해:

?? GitHub Actions(workflow 파일) 수정 시
?? 일반 권한보다 더 강한 권한 (workflow) 필요



D:\spring_react 디렉토리가 깃 저장소인데 
D:\spring_react\react_coffee\node_modules 디렉토리를 깃 관리 대상에서 제외하려면 어떻게 해야 하나요

혹시 다음 디렉토리는 npm run build 명령어로 생긴 디렉토리인데 이 항목은 관리 대상에서 제외해야 하나요
D:\spring_react\react_coffee\dist

react_coffee 디렉토리와 spring_coffee 디렉토리 아래에 .gitignore 파일이 있는 그냥 놔둬도 되나요